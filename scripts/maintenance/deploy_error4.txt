
 鉀咃笍 wrangler 4.67.0 (update available 4.69.0)
鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
鈻?[WARNING] The module rule {"type":"Text","globs":["**/*.html"],"fallbacks":[]} does not have a fallback, the following rules will be ignored:

   {"type":"Text","globs":["**/*.txt","**/*.html","**/*.sql"]} (DEFAULT)
  
  Add `fallthrough = true` to rule to allow next rule to be used or `fallthrough = false` to silence this warning



X [ERROR] Build failed with 8 errors:

  X [ERROR] Could not resolve "fs"
  
      node_modules/better-sqlite3/lib/database.js:2:19:
        2 鈹?const fs = require('fs');
          鈺?                   ~~~~
  
    The package "fs" wasn't found on the file system but is built into node.
    - Make sure to prefix the module name with "node:" or update your compatibility_date to 2024-09-23 or later.
    
  
  
  X [ERROR] Could not resolve "path"
  
      node_modules/better-sqlite3/lib/database.js:3:21:
        3 鈹?const path = require('path');
          鈺?                     ~~~~~~
  
    The package "path" wasn't found on the file system but is built into node.
    - Make sure to prefix the module name with "node:" or update your compatibility_date to 2024-09-23 or later.
    
  
  
  X [ERROR] Could not resolve "fs"
  
      node_modules/better-sqlite3/lib/methods/backup.js:2:19:
        2 鈹?const fs = require('fs');
          鈺?                   ~~~~
  
    The package "fs" wasn't found on the file system but is built into node.
    - Make sure to prefix the module name with "node:" or update your compatibility_date to 2024-09-23 or later.
    
  
  
  X [ERROR] Could not resolve "path"
  
      node_modules/better-sqlite3/lib/methods/backup.js:3:21:
        3 鈹?const path = require('path');
          鈺?                     ~~~~~~
  
    The package "path" wasn't found on the file system but is built into node.
    - Make sure to prefix the module name with "node:" or update your compatibility_date to 2024-09-23 or later.
    
  
  
  X [ERROR] Could not resolve "util"
  
      node_modules/better-sqlite3/lib/methods/backup.js:4:30:
        4 鈹?const { promisify } = require('util');
          鈺?                              ~~~~~~
  
    The package "util" wasn't found on the file system but is built into node.
    - Make sure to prefix the module name with "node:" or update your compatibility_date to 2024-09-23 or later.
    
  
  
  X [ERROR] Could not resolve "fs"
  
      node_modules/bindings/bindings.js:5:17:
        5 鈹?var fs = require('fs'),
          鈺?                 ~~~~
  
    The package "fs" wasn't found on the file system but is built into node.
    - Make sure to prefix the module name with "node:" or update your compatibility_date to 2024-09-23 or later.
    
  
  
  X [ERROR] Could not resolve "path"
  
      node_modules/bindings/bindings.js:6:17:
        6 鈹?  path = require('path'),
          鈺?                 ~~~~~~
  
    The package "path" wasn't found on the file system but is built into node.
    - Make sure to prefix the module name with "node:" or update your compatibility_date to 2024-09-23 or later.
    
  
  
  X [ERROR] Could not resolve "path"
  
      node_modules/file-uri-to-path/index.js:6:18:
        6 鈹?var sep = require('path').sep || '/';
          鈺?                  ~~~~~~
  
    The package "path" wasn't found on the file system but is built into node.
    - Make sure to prefix the module name with "node:" or update your compatibility_date to 2024-09-23 or later.
    
  
  


馃  Logs were written to "C:\Users\hwdem\AppData\Roaming\xdg.config\.wrangler\logs\wrangler-2026-02-27_14-59-12_436.log"
